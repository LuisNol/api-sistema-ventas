<!doctype html>
<html class="no-js" lang="">

<head>
  <meta charset="utf-8">
  <title>Descargar Venta</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }

        /**
        Use a more readable tab size (opinionated).
        */

        :root {
            -moz-tab-size: 4;
            tab-size: 4;
        }

        /**
        1. Correct the line height in all browsers.
        2. Prevent adjustments of font size after orientation changes in iOS.
        */

        html {
            line-height: 1.15; /* 1 */
            -webkit-text-size-adjust: 100%; /* 2 */
        }

        /*
        Sections
        ========
        */

        /**
        Remove the margin in all browsers.
        */

        body {
            margin: 0;
        }

        /**
        Improve consistency of default fonts in all browsers. (https://github.com/sindresorhus/modern-normalize/issues/3)
        */

        body {
            font-family:
                system-ui,
                -apple-system, /* Firefox supports this but not yet `system-ui` */
                'Segoe UI',
                Roboto,
                Helvetica,
                Arial,
                sans-serif,
                'Apple Color Emoji',
                'Segoe UI Emoji';
        }

        /*
        Grouping content
        ================
        */

        /**
        1. Add the correct height in Firefox.
        2. Correct the inheritance of border color in Firefox. (https://bugzilla.mozilla.org/show_bug.cgi?id=190655)
        */

        hr {
            height: 0; /* 1 */
            color: inherit; /* 2 */
        }

        /*
        Text-level semantics
        ====================
        */

        /**
        Add the correct text decoration in Chrome, Edge, and Safari.
        */

        abbr[title] {
            text-decoration: underline dotted;
        }

        /**
        Add the correct font weight in Edge and Safari.
        */

        b,
        strong {
            font-weight: bolder;
        }

        /**
        1. Improve consistency of default fonts in all browsers. (https://github.com/sindresorhus/modern-normalize/issues/3)
        2. Correct the odd 'em' font sizing in all browsers.
        */

        code,
        kbd,
        samp,
        pre {
            font-family:
                ui-monospace,
                SFMono-Regular,
                Consolas,
                'Liberation Mono',
                Menlo,
                monospace; /* 1 */
            font-size: 1em; /* 2 */
        }

        /**
        Add the correct font size in all browsers.
        */

        small {
            font-size: 80%;
        }

        /**
        Prevent 'sub' and 'sup' elements from affecting the line height in all browsers.
        */

        sub,
        sup {
            font-size: 75%;
            line-height: 0;
            position: relative;
            vertical-align: baseline;
        }

        sub {
            bottom: -0.25em;
        }

        sup {
            top: -0.5em;
        }

        /*
        Tabular data
        ============
        */

        /**
        1. Remove text indentation from table contents in Chrome and Safari. (https://bugs.chromium.org/p/chromium/issues/detail?id=999088, https://bugs.webkit.org/show_bug.cgi?id=201297)
        2. Correct table border color inheritance in all Chrome and Safari. (https://bugs.chromium.org/p/chromium/issues/detail?id=935729, https://bugs.webkit.org/show_bug.cgi?id=195016)
        */

        table {
            text-indent: 0; /* 1 */
            border-color: inherit; /* 2 */
        }

        /*
        Forms
        =====
        */

        /**
        1. Change the font styles in all browsers.
        2. Remove the margin in Firefox and Safari.
        */

        button,
        input,
        optgroup,
        select,
        textarea {
            font-family: inherit; /* 1 */
            font-size: 100%; /* 1 */
            line-height: 1.15; /* 1 */
            margin: 0; /* 2 */
        }

        /**
        Remove the inheritance of text transform in Edge and Firefox.
        1. Remove the inheritance of text transform in Firefox.
        */

        button,
        select { /* 1 */
            text-transform: none;
        }

        /**
        Correct the inability to style clickable types in iOS and Safari.
        */

        button,
        [type='button'],
        [type='reset'],
        [type='submit'] {
            -webkit-appearance: button;
        }

        /**
        Remove the inner border and padding in Firefox.
        */

        ::-moz-focus-inner {
            border-style: none;
            padding: 0;
        }

        /**
        Restore the focus styles unset by the previous rule.
        */

        :-moz-focusring {
            outline: 1px dotted ButtonText;
        }

        /**
        Remove the additional ':invalid' styles in Firefox.
        See: https://github.com/mozilla/gecko-dev/blob/2f9eacd9d3d995c937b4251a5557d95d494c9be1/layout/style/res/forms.css#L728-L737
        */

        :-moz-ui-invalid {
            box-shadow: none;
        }

        /**
        Remove the padding so developers are not caught out when they zero out 'fieldset' elements in all browsers.
        */

        legend {
            padding: 0;
        }

        /**
        Add the correct vertical alignment in Chrome and Firefox.
        */

        progress {
            vertical-align: baseline;
        }

        /**
        Correct the cursor style of increment and decrement buttons in Safari.
        */

        ::-webkit-inner-spin-button,
        ::-webkit-outer-spin-button {
            height: auto;
        }

        /**
        1. Correct the odd appearance in Chrome and Safari.
        2. Correct the outline style in Safari.
        */

        [type='search'] {
            -webkit-appearance: textfield; /* 1 */
            outline-offset: -2px; /* 2 */
        }

        /**
        Remove the inner padding in Chrome and Safari on macOS.
        */

        ::-webkit-search-decoration {
            -webkit-appearance: none;
        }

        /**
        1. Correct the inability to style clickable types in iOS and Safari.
        2. Change font properties to 'inherit' in Safari.
        */

        ::-webkit-file-upload-button {
            -webkit-appearance: button; /* 1 */
            font: inherit; /* 2 */
        }

        /*
        Interactive
        ===========
        */

        /*
        Add the correct display in Chrome and Safari.
        */

        summary {
            display: list-item;
        }
            </style>
        <style>
            body {
        font-size: 13px;
        }

        table {
        width: 100%;
        border-collapse: collapse;
        }

        table tr td {
        padding: 0;
        }

        table tr td:last-child {
        text-align: right;
        }

        .bold {
        font-weight: bold;
        }

        .right {
        text-align: right;
        }

        .large {
        font-size: 1.2em;
        }

        .total {
        font-weight: bold;
        color: #fb7578;
        }

        .logo-container {
        margin: 20px 0 30px 0;
        }

        .invoice-info-container {
        font-size: 0.875em;
        }
        .invoice-info-container td {
        padding: 4px 0;
        }

        .client-name {
        font-size: 1.5em;
        vertical-align: top;
        }

        .line-items-container {
        margin: 15px 0;
        font-size: 0.875em;
        }

        .line-items-container th {
        text-align: left;
        color: #999;
        border-bottom: 2px solid #ddd;
        padding: 10px 0 15px 0;
        font-size: 0.75em;
        text-transform: uppercase;
        }

        .line-items-container th:last-child {
        text-align: right;
        }

        .line-items-container td {
        padding: 5px 0;
        }

        /* Tabla de productos con bordes */
        .products-table {
        margin: 15px 0;
        font-size: 0.875em;
        border: 1px solid #333;
        border-collapse: collapse;
        }

        .products-table th {
        text-align: left;
        color: #000;
        border: 1px solid #333;
        padding: 8px;
        font-size: 0.75em;
        text-transform: uppercase;
        background-color: #f5f5f5;
        }

        .products-table th:last-child {
        text-align: right;
        }

        .products-table td {
        padding: 8px;
        border: 1px solid #333;
        }

        .products-table .importe-letras-row td {
        padding: 8px !important;
        border: 1px solid #333 !important;
        background-color: #ffffff !important;
        font-weight: normal !important;
        text-align: left !important;
        }

        .line-items-container.has-bottom-border tbody tr:last-child td {
        padding-bottom: 25px;
        border-bottom: 2px solid #ddd;
        }

        .line-items-container.has-bottom-border {
        margin-bottom: 0;
        }

        .line-items-container th.heading-quantity {
        width: 50px;
        }
        .line-items-container th.heading-price {
        text-align: right;
        width: 100px;
        }
        .line-items-container th.heading-subtotal {
        width: 100px;
        }

        .payment-info {
        width: 38%;
        font-size: 0.75em;
        line-height: 1.5;
        }

        .footer {
        margin-top: 30px;
        }

        .footer-thanks {
        font-size: 1.125em;
        }

        .footer-thanks img {
        display: inline-block;
        position: relative;
        top: 1px;
        width: 16px;
        margin-right: 4px;
        }

        .footer-info {
        float: right;
        margin-top: 5px;
        font-size: 0.75em;
        color: #ccc;
        }

        .footer-info span {
        padding: 0 5px;
        color: black;
        }

        .footer-info span:last-child {
        padding-right: 0;
        }

        .page-container {
        display: none;
        }
        .page-break {
            page-break-after: always;
        }

        .number-clausulas{
            /* display: -webkit-inline-box;
            display: -webkit-box; */
        }
        .number-clausulas p{
            margin: 0;
            text-align: justify;
            font-size: 0.76rem; /*0.68rem*/;
        }
        .number-clausulas strong{
            float: left;
            font-size: 0.76rem; /*0.68rem*/;
        }
        .number-clausulas ul li{
            font-size: 0.76rem; /*0.68rem*/;
        }
        .place-date{
            text-align: right;
        }
        .place-date p{
            /* font-size: small; */
            font-size: 0.6rem;
        }
  </style>
</head>
<body>

<div class="web-container">

  <!-- See invoice.html! It is injected here... -->
  <div class="page-container">
    Page
    <span class="page"></span>
    of
    <span class="pages"></span>
  </div>

  <div class="logo-container">
    <table>
        <tbody>
            <tr>
                <td style="padding: 0 !important;border-bottom:none;">
                    <img
                        style="height: 75px;background:black;"
                        src="{{ public_path('laravest.png') }}"
                    >
                </td>
                
                <td style="padding: 0 !important;border-bottom:none;">
                    N° {{$sale->state_sale == 1 ? 'VENTA' : 'COTIZACION'}}: <strong>#{{$sale->id}}</strong>
                    <br>
                    <img
                        style="width:130px;background:black;"
                        src="{{ public_path('laravest.png') }}"
                    >
                    <br>
                    <small>https://admin-fe.avisonline.store/</small>
                    <br>
                    <small>nolberto.sumaran@gmail.com</small>
                </td>
            </tr>
        </tbody>
    </table>
  </div>
  <div style="clear:both;"></div>
  <table class="invoice-info-container">
    <tr>
        <td>
          N° {{$sale->state_sale == 1 ? 'VENTA' : 'COTIZACION'}}: <strong>#{{$sale->id}}</strong>
        </td>

        <td>
          FECHA CONTRATO: {{$sale->created_at->format("Y/m/d")}}
        </td>
    </tr>
    <div class="" style="display: block;width:100%;border:1px solid black;height:1px;"></div>
    <tr>
      <td class="">
        <b>Datos del cliente: </b>
        <br>
        <br>
        CLIENTE: {{$sale->client->full_name}}
            <br>
            DEPART./PROVINCIA CLIENTE: <strong>{{$sale->client->region}}/{{$sale->client->provincia}}/{{$sale->client->distrito}}</strong>
      </td>
      <td>
        {{$sale->client->type_document}}:  {{$sale->client->n_document}}
      </td>
    </tr>
    <tr>
        <td>TIPO CLIENTE: {{$sale->client->type_client == 1 ? 'CLIENTE FINAL' : 'CLIENTE EMPRESA'}}</td>
        <td>TELÉFONO: {{$sale->client->phone}}</td>
    </tr>
    <div class="" style="display: block;width:100%;border:1px solid black;height:1px;"></div>
    <tr>
        <td>
         VENDEDOR: <strong> {{$sale->user->name.' '.$sale->user->surname}}</strong>
        </td>
        <td>
            TELÉFONO: {{$sale->user->phone}}
        </td>
    </tr>

  </table>

  <table class="products-table">
    <thead>
      <tr>
        <th style="width: 80px;">Código</th>
        <th style="width: 40px;">Cant</th>
        <th style="width: 50px;">Unidad</th>
        <th class="heading-description">Descripción</th>
        <th class="heading-price">Valor U.</th>
        <th class="heading-price">Precio U.</th>
        <th class="heading-price">IGV</th>
        <th class="heading-price">Descuento</th>
        <th class="heading-price">SubTotal</th>
      </tr>
    </thead>
    <tbody>
        @foreach ($sale->sale_details as $sale_detail)
            <tr>
                <td>{{ $sale_detail->product->sku ?? str_pad($sale_detail->product_id, 8, '0', STR_PAD_LEFT) }}</td>
                <td style="text-align: center;">{{ $sale_detail->quantity }}</td>
                <td style="text-align: center;">{{ strtoupper($sale_detail->unidad_medida ?? 'UND') }}</td>
                <td>{{ $sale_detail->product->title ?? $sale_detail->description }}</td>
                <td class="right">{{ number_format($sale_detail->price_base ?? 0, 2) }}</td>
                <td class="right">{{ number_format($sale_detail->price_final ?? 0, 2) }}</td>
                <td class="right">{{ number_format($sale_detail->igv ?? 0, 2) }}</td>
                <td class="right">{{ number_format($sale_detail->discount ?? 0, 2) }}</td>
                <td class="right">{{ number_format($sale_detail->subtotal ?? 0, 2) }}</td>
            </tr>
        @endforeach
        @php
            $sales_anticipos = $sale->sales_anticipos ? json_decode($sale->sales_anticipos,true) : [];
        @endphp
        @foreach ($sales_anticipos as $sales_anticipo)
            <tr>
                <td colspan="2"></td>
                <td colspan="5">Anticipo de la factura "{{$sales_anticipo["n_operacion"]}}"</td>
                <td class="right">- {{number_format($sales_anticipo["subtotal"] ?? 0, 2)}}</td>
                <td class="right">- {{number_format($sales_anticipo["total"] ?? 0, 2)}}</td>
            </tr>
        @endforeach
        
        @php
            try {
                $converter = new \Luecano\NumeroALetras\NumeroALetras();
                $entero = floor($sale->subtotal ?? 0);
                $decimales = str_pad(round((($sale->subtotal ?? 0) - $entero) * 100), 2, '0', STR_PAD_LEFT);
                $texto_entero = $converter->toWords($entero);
                $total_letras = strtoupper($texto_entero) . ' (' . $decimales . '/100) Soles';
            } catch (\Exception $e) {
                $entero = floor($sale->subtotal ?? 0);
                $decimales = str_pad(round((($sale->subtotal ?? 0) - $entero) * 100), 2, '0', STR_PAD_LEFT);
                $total_letras = number_format($entero, 0) . ' (' . $decimales . '/100) Soles';
            }
        @endphp
        
        <tr class="importe-letras-row">
            <td colspan="9">
                <strong>IMPORTE EN LETRAS:</strong> {{ $total_letras }}
            </td>
        </tr>
    </tbody>
  </table>


  <table class="line-items-container has-bottom-border">
    <thead>
      <tr>
        <th>Metodo de Pago</th>
        <th>Fecha Entrega</th>
        <th>Información de Pago</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="payment-info">
            <div>
                METODO DE PAGO: <strong> 
                    @if ($sale->first_payment)
                    {{$sale->first_payment->method_payment}}  {{$sale->currency}}  {{$sale->first_payment->amount}} 
                    @endif
                </strong>
            </div>
        </td>
        <td class="large">{{$sale->date_validation ? Carbon\Carbon::parse($sale->date_validation)->format("Y/m/d h:i A") : NULL}}</td>
        @php
            $discount_global_general = 0;
            if($sale->discount_global){
                $discount_global_general += $sale->discount_global;
            }
            if($sale->amount_anticipo){
                $discount_global_general += $sale->amount_anticipo;
            }
            $retencion_igv_total = 0;$percepcion_igv_total = 0;
            if ($sale->retencion_igv == 3) {
                $percepcion_igv_total = round(($sale->total * 0.04),2);  
            }
            switch ($sale->retencion_igv) {
                case 1://Retención
                    $retencion_igv_total = round(($sale->total * 0.03),2);
                    break;
                case 2://Detracción
                    $retencion_igv_total = round(($sale->total * 0.04),2);
                    break;  
                default:
                    break;
            }

            $icbper_total = round($sale->sale_details->sum('icbper'),2);
            $isc_total = round($sale->sale_details->sum('isc'),2);

            $getTotalSales = round(($sale->total + $icbper_total + $isc_total + $percepcion_igv_total) - ($discount_global_general + $sale->igv_discount_general + $retencion_igv_total),2);
            
            $getSubTotalSale = round($sale->subtotal - $discount_global_general,2);

            $getIgvTotal = round($sale->igv - $sale->igv_discount_general,2); //- $IGV_PERCEPCION;
        @endphp
        <td class="payment-info">
            <div class="large total">
                DESCUENTO: - {{$sale->currency}} {{$sale->discount + $sale->discount_global}}
                <br>
                @if ($sale->amount_anticipo > 0)
                    ANTICIPO: - {{$sale->currency}} {{$sale->amount_anticipo}}
                    <br>
                @endif
                SUBTOTAL: {{$sale->currency}} {{$getSubTotalSale}}
                @if ($isc_total > 0)
                    <br>
                    ISC: {{$sale->currency}} {{$isc_total}}
                @endif
                <br>
                IGV: {{$sale->currency}} {{$getIgvTotal}}
                <br>
                TOTAL: {{$sale->currency}} {{ $getTotalSales}}
            </div>
            @switch($sale->retencion_igv)
                @case(1)
                    <div>
                        RETENCIÓN: <strong>{{$sale->currency}} {{round(($sale->total * 0.03),2)}} </strong>
                    </div>
                    @break
                @case(2)
                    <div>
                        DETRACCIÓN: <strong>{{$sale->currency}} {{round(($sale->total * 0.04),2)}} </strong>
                    </div>
                    @break
                @case(3)
                    <div>
                        PERCEPCIÓN: <strong>{{$sale->currency}} {{round(($sale->total * 0.04),2)}} </strong>
                    </div>
                    @break
                @default
                    
            @endswitch
            @if ($icbper_total > 0)
            <div>
                ICPER: <strong>{{$sale->currency}} {{$icbper_total}}</strong>
            </div>
            @endif
            <div>
              ADELANTADO: <strong>{{$sale->currency}} {{$sale->paid_out}}</strong>
            </div>
            <div>
              SALDO: <strong>{{$sale->currency}} {{$sale->debt}}</strong>
            </div>
        </td>
    </tbody>
  </table>

    <div class="footer">
        <div class="footer-info">
            <span> ANOTACIONES FINALES: {{$sale->description}} </span>
        </div>
    </div>

</div>


</body></html>
